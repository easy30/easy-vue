// Minimal stub for "@vue/devtools-api" (used by vue-router's ESM build).
// The demo doesn't use Vue DevTools, so these are no-ops.
export function setupDevtoolsPlugin(_pluginDescriptor, _setupFn) {}
